# GitHub Release Build

The `Release` workflow builds Cherry Studio Pi installers for macOS, Windows, and Linux, then uploads them to a GitHub Release.

## Trigger

Run it in either way:

```bash
git tag v1.9.7
git push origin v1.9.7
```

Or open **Actions -> Release -> Run workflow** and provide a tag such as `v1.9.7`.

Manual runs can build `all`, `mac`, `windows`, or `linux`. Tag pushes always build all platforms.

## Output

The release includes the installers generated by `electron-builder`, update metadata, blockmaps, and SHA256 checksum files:

- macOS: `.dmg`, `.zip`
- Windows: setup `.exe`, portable `.exe`
- Linux: `.AppImage`, `.deb`, `.rpm`
- Update metadata: `latest*.yml`
- Checksums: `SHA256SUMS-*.txt`

## Required Repository Settings

In **Settings -> Actions -> General**, make sure workflow permissions allow:

- Read and write permissions

No custom token is required for normal releases. The workflow uses the built-in `GITHUB_TOKEN`.

## Optional Secrets

Build-time app secrets:

- `MAIN_VITE_CHERRYAI_CLIENT_SECRET`
- `MAIN_VITE_MINERU_API_KEY`
- `RENDERER_VITE_AIHUBMIX_SECRET`
- `RENDERER_VITE_PPIO_APP_SECRET`

macOS signing and notarization:

- `CSC_LINK`
- `CSC_KEY_PASSWORD`
- `APPLE_ID`
- `APPLE_APP_SPECIFIC_PASSWORD`
- `APPLE_TEAM_ID`

Windows signing:

- `WIN_SIGN`
- `CHERRY_CERT_PATH`
- `CHERRY_CERT_KEY`
- `CHERRY_CERT_CSP`
- `WIN_SIGN_TIMESTAMP_URLS`

If signing secrets are not configured, the workflow still builds unsigned/ad-hoc artifacts where supported.
